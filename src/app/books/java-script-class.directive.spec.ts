import { JavaScriptClassDirective } from './java-script-class.directive';

describe('JavaScriptClassDirective', () => {
  it('should create an instance', () => {
    const directive = new JavaScriptClassDirective();
    expect(directive).toBeTruthy();
  });
});
